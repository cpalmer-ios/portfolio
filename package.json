{
  "name": "potfolio",
  "version": "1.0.0",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "description": "",
  "scripts": {
    "start": "parcel ./index.html --open",
    "clean": "rm -rf dist/*",
    "build:parcel": "parcel build ./index.html --no-content-hash --no-source-maps --public-url ./",
    "build": "npm run clean && npm run build:parcel && touch dist/.nojekyll",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist --dotfiles"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/cpalmer-ios/portfolio/[NAME].git"
  },
  "keywords": [],
  "author": "Codrops",
  "license": "MIT",
  "homepage": "https://web-dev.uk",
  "bugs": {
    "url": "https://github.com/cpalmer-ios/portfolio/[NAME]/issues"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "gsap": "2.1.3",
    "parcel": "latest"
  }
}
